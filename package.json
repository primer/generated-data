{
  "name": "generated-data",
  "version": "0.0.0",
  "description": "A repo with generated GitHub-like data to be used in tools like Figma plugins and prototypes.",
  "type": "module",
  "main": "lib/index.js",
  "files": [
    "dist",
    "lib"
  ],
  "scripts": {
    "build": "npm run clean && tsc && npm run copy:asstes",
    "clean": "rm -rf dist && rm -rf lib",
    "copy:asstes": "mkdir dist && cp -r assets dist/assets",
    "test": "echo \"Error: no test specified\" && exit 1",
    "preversion": "echo \"Warning: No tests specified\" && exit 0",
    "version": "npm run build && git add -A dist lib",
    "postversion": "gith push && git push tags && npm publish"
  },
  "keywords": [],
  "author": "Github Inc.",
  "license": "MIT",
  "dependencies": {
    "@faker-js/faker": "^9.0.1"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
